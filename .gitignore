# Rust — workspace-level Cargo.lock is committed (the workspace produces a binary)
target/

# Node
node_modules/
dist/

# Tauri
apps/desktop/src-tauri/gen/

# Local MCP registration -- .mcp.json's absolute binary path is
# machine-specific (points at this checkout's target/debug/magpie), not
# something to commit for other contributors.
.mcp.json

# OS
.DS_Store

# Editors
.vscode/*
!.vscode/extensions.json
.idea/

# Local agent tooling -- session-local planning/config, not project docs
.claude/
docs/superpowers/
