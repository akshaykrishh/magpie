allowBuilds:
  esbuild: true
  sharp: true
  unrs-resolver: true

# Security patches for transitive dependencies pulled in by next/eslint's
# own dependency trees -- see the Dependabot alerts these close.
overrides:
  postcss: 8.5.25
  sharp: 0.35.3
  fast-uri: 3.1.5
